import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Accept invite | Dinaya",
};

export default function AcceptInviteLayout({ children }: { children: React.ReactNode }) {
  return children;
}
